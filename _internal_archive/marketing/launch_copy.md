# Launch Copy Bank

## Primary tagline
**Stop making AI read your entire repository.**

## One-liners (pick per channel)
- Repository intelligence for AI coding — JARVIS prepares your codebase so Claude, Codex and Cursor understand it instantly.
- Your AI agent re-reads your whole repo every time. JARVIS hands it a map instead.
- Architecture maps, dependency graphs, impact analysis and AI-ready context — before your agent sees a single file.
- Make Claude, Codex and Cursor fluent in your codebase. Locally. Deterministically.

## Elevator pitch (≈30 seconds)
Every time you ask Claude, Codex or Cursor a question about your codebase, it burns
thousands of tokens re-reading files just to orient itself — and still guesses at
structure it never fully learned. JARVIS analyzes your repository **once**, locally,
and builds the maps an AI actually needs: subsystems, a 3D dependency graph,
architectural risk, change-impact, and a compact context packet. You paste that
packet into your tool, and suddenly it answers faster, cheaper, and with evidence.
It doesn't replace your AI — it makes the one you already use dramatically smarter.

## Copywriting rules (premium voice)
| Avoid (tool-speak) | Prefer (product-speak) |
|---|---|
| Analyze Repository | Understand your architecture |
| Dependency Graph | Explore your code universe |
| Run scan | Map your repository |
| Risk report | Find your architectural risks |
| Export context | Prepare your codebase for AI |
| Bug finder | Investigate from a trace |
| Token reduction | Cheaper, faster AI answers |

## Value props (for feature sections)
1. **Repository Intelligence** — plain-English architecture, subsystems, entry points, runtime flow.
2. **Dependency Universe** — an interactive 3D map of how your code connects.
3. **Impact Analysis** — the blast radius of a change, before you make it.
4. **Architecture Risk Detection** — the riskiest modules, ranked with evidence.
5. **Claude / Codex / Cursor Export** — one click to a compact, token-cheap context packet.

## Proof points (only claim what's true)
- Local-first — your code never leaves your machine. No cloud, no API keys.
- Deterministic — same input, same output; evidence over guesses.
- Real dependency graph + architectural risk engine under the hood (not an LLM wrapper).

## CTA bank
- Join the waitlist → be first to prepare your codebase for AI.
- Watch the 60-second demo.
- Apply for the private beta.
